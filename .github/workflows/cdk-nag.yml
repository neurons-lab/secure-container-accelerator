name: NAG Check

on: [push]
permissions:
      id-token: write   # This is required for requesting the JWT
      contents: read    # This is required for actions/checkout

env:
  AWS_REGION : "us-east-1"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: configure aws credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        role-to-assume: arn:aws:iam::433559402488:role/GitHubActionCDKRole
        role-session-name: samplerolesession
        aws-region: ${{ env.AWS_REGION }}
    - name:  Test Access to s3
      run: |
          aws s3 ls
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: |
        npm install -g aws-cdk
        pip install -r deploy/requirements.txt
    - name: Build
      run: |
        cd deploy
        cdk synth
#    - name: Stelligent cfn_nag
#      uses: stelligent/cfn_nag@v0.8.6
#      with:
#        input_path: deploy/cdk.out/
#        extra_args: -t ..*\.template\.json
#    - uses: github/codeql-action/upload-sarif@v1
#      with:
#        sarif_file: cfn_nag.sarif

